mixin select-tree(records, level=0, selectedId='')
    each item in records
        - const prefix = '-- '.repeat(level)
        option(
            value=item._id
            selected=item._id == selectedId
        ) #{prefix} #{item.title}
        if item.children
            +select-tree(item.children, level + 1)