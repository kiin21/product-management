mixin search(keyword)
    form(id='searchForm')
        .input-group 
            input(
                type="text"
                placeholder="Search..."
                name="keyword"
                class="form-control"
                value=keyword?keyword:''
            )
            .input-group-append 
                button(
                    type="submit"
                    class="btn btn-success"
                ) Search