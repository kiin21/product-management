mixin filterBar(filterBar)
    each button in filterBar

        button(
            class=`btn btn-sm me-1 btn-outline-success ${button.class}`
            button-status=button.status
        ) #{button.name} 